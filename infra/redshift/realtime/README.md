# Redshift realtime warehouse SQL

Phase 7 owns Redshift Serverless `raw_cdc` and `cdc_audit` bootstrap and
migration SQL here. It must preserve the same logical audit contract as the
local PostgreSQL adapter.
