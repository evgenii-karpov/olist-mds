# Deterministic workload simulator

Phase 1 owns the simulator package and stable `seed`, `replay`, `run`, `status`,
and `stop` interfaces here. Runtime state and generated secrets are ignored and
must never be committed.
