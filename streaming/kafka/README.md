# Kafka assets

Phase 2 owns explicit topic definitions, KRaft broker configuration, retention
policies, and validation fixtures here. Runtime image versions are centralized
in `streaming/runtime-versions.json`.
